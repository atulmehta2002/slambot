twist_mux:
  ros__parameters:
    topics:
      keyboard_manual:
        topic   : cmd_vel_manual
        timeout : 0.5
        priority: 10
      keyboard:
        topic   : cmd_vel
        timeout : 0.5
        priority: 20
      nav2_controller:
        topic   : cmd_vel_nav
        timeout : 0.5
        priority: 30
      nav2_velocity_smoother:
        topic   : cmd_vel_smoothed
        timeout : 0.5
        priority: 40
      # tracker:
      #   topic   : cmd_vel_tracker
      #   timeout : 0.5
      #   priority: 20
      # joystick:
      #   topic   : cmd_vel_joy
      #   timeout : 0.5
      #   priority: 100